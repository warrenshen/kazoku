<%= react_component("Header", {currentPerson: current_person, homePath: root_path, loginPath: login_path, personPath: person_path(current_person), isColored: true}) %>
<section class="person-banner">
  <div class="person-banner-content">
    <h2 class="person-banner-name">
      <%= @person.first_name + " " + @person.last_name %>
    </h2>
  </div>
</section>
